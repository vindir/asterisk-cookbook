[general]
static=<%= node['asterisk']['extensions']['static'] %>
writeprotect=<%= node['asterisk']['extensions']['writeprotect'] %>
autofallthrough=<%= node['asterisk']['extensions']['autofallthrough'] %>
clearglobalvars=<%= node['asterisk']['extensions']['clearglobalvars'] %>
priorityjumping=<%= node['asterisk']['extensions']['priorityjumping'] %>

<% @dialplan_contexts.each do |context| %>
[<%= context['name'] %>]
<% context['entries'].each do |entry| %>
<%= entry %>
<% end %>

<% end %>
