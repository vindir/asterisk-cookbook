[general]
static=<%= ['asterisk']['extensions']['static'] %>
writeprotect=<%= ['asterisk']['extensions']['writeprotect'] %>
autofallthrough=<%= ['asterisk']['extensions']['autofallthrough'] %>
clearglobalvars=<%= ['asterisk']['extensions']['clearglobalvars'] %>
priorityjumping=<%= ['asterisk']['extensions']['priorityjumping'] %>

<% @dialplan_contexts.each do |context| %>
[<%= context['name'] %>]
<% context['entries'].each do |entry| %>
<%= entry %>
<% end %>

<% end %>
